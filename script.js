$(window).on("scroll", function() {
    // Simple scroll-based animations can be added here
});


// Initialize carousel with optional settings
var myCarousel = document.querySelector('#customCarousel')
var carousel = new bootstrap.Carousel(myCarousel, {
  interval: 3000,  // Auto-slide every 3 seconds
  wrap: true       // Allow the carousel to loop continuously
})


var carousel = new bootstrap.Carousel(document.getElementById('carouselExample'), {
    interval: 3000, // 3 seconds
    ride: 'carousel'
  });




  //GALLR

